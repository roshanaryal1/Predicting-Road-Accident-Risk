# Final Submission Checklist

Everything automatable is done and every open decision is resolved. What's
left needs you specifically. Full day-by-day plan is in `PROGRESS.md` —
this is the short version.

## Done

- [x] All evidence docs (`IMPACT.md`, `RESPONSIBLE_AI.md`,
      `MODEL_VALIDATION.md`) written and cross-checked
- [x] SDG + Responsible AI live on the deployed app (verified 11 Aug 2026)
- [x] Gauge chart bug fixed and verified live
- [x] Risk-concentration numbers visible in-app, not just in docs
- [x] Real official Application Form found, filled within all word
      limits, rendered to PDF — `APPLICATION_FORM_FILLED.pdf`
- [x] Email resolved: roshanaryaal@gmail.com
- [x] Channel resolved: both email (info@smartsust.org) and the Google
      Form portal
- [x] `SSD2026_submission.zip` built and current — real form PDF, write-up
      PDF, 3 live screenshots, 3 supporting docs
- [x] Claude attribution stripped from all 5 commits in repo history,
      global rule set so this never happens again in any repo

## Left — needs you

1. **Record the video** — `VIDEO_SCRIPT.md` has the script. Open the app
   a minute before recording.
2. **Add it to the zip** — drop the file in `submission_package/`, tell
   me, I'll rebuild the zip.
3. **Read both PDFs once** before sending — `APPLICATION_FORM_FILLED.pdf`
   (the real form) and `PROJECT_SUBMISSION.pdf` (fuller write-up).
4. **Send both channels:**
   - Email `APPLICATION_FORM_FILLED.pdf` + `SSD2026_submission.zip` to
     info@smartsust.org, subject `Aryal_R_SSD2026_ProjectSubmission`
   - Submit via the Google Form, upload `SSD2026_submission.zip`, use the
     4 field answers in `APPLICATION_DRAFT.md`
5. **Keep your own confirmation** of both sends.

## Backlog — not before submission

- **Custom domain (roshanaryal.com) for the live demo.** Real idea, but
  DNS/cert changes touching the one working demo link days before
  sending it is pure downside risk for zero evidentiary benefit — do
  this after 15 Aug, not before.
- Uncertainty/confidence-interval UI polish (already disclosed honestly
  in `RESPONSIBLE_AI.md` as not-yet-built — fine to leave)

Target: 15 Aug 2026.
